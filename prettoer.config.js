const options = {
    arrowParans: 'avoid',
    singleQuote: true,
    bracketSpacing: true,
    endOfLine: "if",
    semi: false,
    tabWidth: 2,
    tranilingComma: 'none'
}

module.exports = options